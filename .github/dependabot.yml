version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    cooldown:
      default-days: 7   # let a new release age a week before proposing it
    labels: ["type: chore"]
    commit-message:
      prefix: ci
    groups:
      actions:
        patterns: ["*"]
