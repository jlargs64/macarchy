-- rose-pine -- ported from Omarchy github.com/omacom/omarchy (quattro) themes/rose-pine/neovim.lua @ 947e2fc (via local --src checkout, not curl)
return {
  {
    "rose-pine/neovim",
    lazy = false,
    priority = 1000,
  },
  { "LazyVim/LazyVim", opts = { colorscheme = "rose-pine-dawn" } },
}
