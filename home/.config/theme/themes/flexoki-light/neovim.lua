-- flexoki-light -- ported from Omarchy github.com/omacom/omarchy (quattro) themes/flexoki-light/neovim.lua @ 947e2fc (via local --src checkout, not curl)
return {
  {
    "kepano/flexoki-neovim",
    lazy = false,
    priority = 1000,
  },
  { "LazyVim/LazyVim", opts = { colorscheme = "flexoki-light" } },
}
